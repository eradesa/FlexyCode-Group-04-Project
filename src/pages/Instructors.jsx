import React from 'react';
import Header from '../components/global/MessageHeader';

// eslint-disable-next-line react/prop-types
const Analytics = ({isDarkMode}) => {
    return (
        <React.Fragment>
            <Header isDarkMode={isDarkMode} title="Instructors" subtitle="Instructor Detail Page" />
            <div>
                
            </div>
          
        </React.Fragment>
      );
};

export default Analytics;